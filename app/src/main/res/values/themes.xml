<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidExample" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/txt_primary</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="toolbarStyle">@style/CustomToolbarStyle</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray_200</item>
        <item name="colorSecondaryVariant">@color/gray_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="buttonStyle">@style/CustomButton</item>
        <item name="android:buttonStyle">@style/CustomAppCompatButton</item> <!-- Ensures compatibility -->
        <item name="materialButtonStyle">@style/CustomButton</item> <!-- For MaterialButton -->

        <!-- Customize your theme here. -->
    </style>

    <style name="CustomToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="titleTextColor">@color/txt_primary</item>  <!-- Forces black title -->
    </style>
</resources>