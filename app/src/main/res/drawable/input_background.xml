<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Light gray background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/input_bk_divider" />
        </shape>
    </item>

    <!-- Dark gray line at the bottom -->
    <item android:right="0dp" android:left="0dp" android:top="0dp" android:bottom="1dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/input_bk" />
        </shape>
    </item>
</layer-list>